# ひろば掲示板 要件書

## 目的
誰でも投稿でき、様々な意見を交換できる掲示板。投稿するとポイントがたまり、レベル・バッジ・ランキングで
「投稿する理由」が生まれる仕組みにする。

## 使い方
[supabase/セットアップ手順.md](supabase/セットアップ手順.md) で Supabase を用意し、`config.js` に接続情報を書く。
その後は `index.html` をブラウザで開くだけ(ビルド不要。GitHub Pages にそのまま置ける)。

## 構成
| ファイル | 役割 |
|---|---|
| `store.js` | データ層。Supabase との通信と、画面用のキャッシュ。画面はここだけを通してデータを扱う |
| `app.js` | 画面(ハッシュルーティング)。データは必ず `Store` 経由で扱う |
| `style.css` | 見た目(スマホ対応・ダークモード対応) |
| `config.js` | Supabase の接続情報(Project URL と anon キー。公開してよい値のみ) |
| `supabase/schema.sql` | テーブル・関数・権限。ポイント計算・権限チェック・NGワード判定・連続投稿制限はここ(サーバー側) |
| `vendor/supabase-js.umd.js` | 公式クライアント(npm 配布物を SHA-512 で検証して同梱。CDN には依存しない) |

## バックエンド(Supabase)の設計
- **認証**: Supabase Auth(メール+パスワード)。登録時にトリガーでプロフィールを作成し、+10pt。
  最初に登録した人が管理者。メール確認は Supabase 側の設定で ON/OFF(ON 推奨)。
- **データ**: profiles / threads / posts / post_likes / post_reports / points_log / ng_words。
  すべて RLS 有効・ポリシーなし = **ブラウザからの直接アクセスは全面禁止**。
- **API**: すべて `security definer` の関数(RPC)。読み取りは list_threads / get_thread / ranking / get_me など、
  書き込みは create_thread / create_reply / toggle_like / set_best / report_post / daily_bonus、管理者用は admin_*。
  関数内で「ログイン済みか」「管理者か」「自分の投稿か」を検査する。内部ヘルパー(add_points 等)は実行権限を外してあり、直接呼べない。
- **時刻**: 「1日」の区切りは日本時間(Asia/Tokyo)。

## 機能
- アカウント: メール+パスワードで登録/ログイン。表示名はニックネーム。最初に登録した人が管理者。
- 掲示板: カテゴリ別スレッド、返信、`>>番号` での返信リンク、検索、並び替え(新着/人気/返信待ち)。
- インセンティブ: ポイント、レベル(8段階)、バッジ(8種)、総合/月間ランキング、ポイント履歴、レベルアップ演出。
- ベストアンサー: スレッドを立てた人が返信から1つ選べる(選ばれた人に +10pt)。
- 荒らし対策: NGワード(投稿拒否)、通報(3件で自動非表示)、管理者による非表示/復元、連続投稿の間隔制限。

## 「投稿したくなる」ための設計
| 仕掛け | 内容 |
|---|---|
| その場で書ける投稿欄 | ホームの最上部。タイトルをクリックすると本文・カテゴリが展開。ページ移動不要 |
| 下書きの自動保存 | 書きかけをブラウザ(localStorage)に保存。誤ってページを離れても消えない |
| 今日のお題 | 毎日1つ、10種類から日替わり。ワンタップで下書きに入り、書き出しの負担を減らす |
| 今日のミッション | 「返信する」「スレッドを立てる」。両方やると +3pt。達成状況をサイドバー/マイページ/ホームに表示 |
| 返信待ちの可視化 | 返信ゼロのスレッドに「返信を待っています +2pt」。「返信待ち」の並び替えで一覧できる |
| ポイントの即時フィードバック | 獲得ごとにトースト、ヘッダーのポイントが弾む、レベルアップは祝福演出 |
| 文字数ヒント | 本文が10文字未満なら「あと○文字でポイント対象」と表示 |

## 動線・操作性
- スマホ: 下タブ(ホーム/ランキング/**中央の投稿ボタン**/ポイント/マイページ)。親指だけで主要操作が届く。
- PC: 上部ナビ+右サイドバー(進捗・ミッション・今日のお題・月間ランキング)。
- スレッド画面: 画面下に追従する「返信を書く」バー(返信欄が見えているときは自動で隠れる)、各投稿の「返信」で `>>番号` を自動入力、
  ページ下部に「他のスレッドも見てみよう」を表示して回遊を促す。
- Ctrl+Enter(Mac は Cmd+Enter)で送信。再描画しても入力中の返信は消えない。
- アクセシビリティ: キーボード操作・フォーカス表示・スクリーンリーダー用ラベル・`prefers-reduced-motion` 対応。

## ポイントのルール(正は `schema.sql` の `rule_pts()`。画面表示用に `store.js` の `RULES` も同じ値にする)
| 行動 | ポイント |
|---|---|
| 新規登録 | +10 |
| ログインボーナス(1日1回) | +1 |
| スレッドを立てる | +5 |
| 返信する | +2 |
| その日「スレッド」と「返信」の両方をやる | +3(ミッションボーナス) |
| いいねをもらう | +1(取り消されたら -1) |
| ベストアンサーに選ばれる | +10(取り消されたら -10) |
| 管理者に非表示にされる | -10(復元で返却) |

不正対策(すべてサーバー側で判定): 投稿ポイントは1日30ptまで / 10文字未満はポイント対象外(ミッションにも数えない) /
自分の投稿へのいいね・ベストアンサー不可 / 20秒間隔。

## 残っている課題
- **複数アカウントでのポイント稼ぎ(自作自演)**は完全には防げない。メール確認を ON にすると大きく減らせる。
  さらに必要なら、端末/IP 単位の制限や、不自然なポイント推移の検知を追加する。
- パスワード再設定の画面がない(Supabase の機能はあるが、画面は未実装)。
- 一覧は新しい順に最大200件まで。件数が増えたらページ送り・サーバー側検索が必要。検索はタイトルと本文の冒頭100文字が対象。
- 投稿者本人による編集・削除は未対応(管理者の非表示のみ)。
- 表示は全て HTML エスケープ済み(XSS 対策)。リンクの自動変換・画像投稿は未対応。
- 無料プランは1週間アクセスがないと一時停止される。メール送信の上限も小さい(独自 SMTP で解消)。

## 今後の候補
- 通知(返信・いいね・ベストアンサーされたとき)、画像添付、パスワード再設定、投稿の編集・削除
- 連続ログイン日数(ストリーク)、週間ミッション
- ポイントの使い道(整えサロンの割引特典など)※ 運用ルールの検討が必要
